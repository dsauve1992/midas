{
  "name": "root",
  "private": true,
  "npmClient": "yarn",
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {},
  "devDependencies": {
    "lerna": "^7.2.0"
  },
  "scripts": {
    "start:dev": "lerna run start:dev --stream"
  }
}
